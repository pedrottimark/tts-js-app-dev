var teachers = ['Assaf', 'Shane', 'Zack'];

console.log('array before pop:         ', teachers);

var popped = teachers.pop();

console.log('pop returns the last item:', popped);
console.log('array after pop:          ', teachers);
