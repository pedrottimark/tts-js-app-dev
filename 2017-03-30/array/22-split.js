teachers = ['Assaf', 'Shane', 'Zack', 'Dale, Jr.'];

console.log('array:      ', teachers);

var joined = teachers.join(', ');
var split = joined.split(', ');

console.log("join(', ')  ", joined);
console.log("split(', ') ", split); // Uh, oh,
// when I assume, I make an ass of you and me!