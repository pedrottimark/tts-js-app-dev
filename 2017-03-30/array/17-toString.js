teachers = ['Assaf', 'Shane', 'Zack'];

console.log('array:          ', teachers);

var string = teachers.toString();

// Less recommended because less flexible to join an array
console.log('array.toString()', string);