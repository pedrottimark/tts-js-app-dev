var course = {
  name: 'JavaScript Applications',
  awesome: true,
  teachers: ['Assaf', 'Shane']
};

console.log('course name before:', course.name);

course.name = 'super duper class';

console.log('course name after: ', course.name);